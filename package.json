{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npm run build:icons && npm run build:popup",
        "build:icons": "node scripts/generate-icons.mjs",
        "build:popup": "rolldown -c rolldown.config.mjs"
    },
    "devDependencies": {
        "chrome-webstore-upload-cli": "^3.0.0",
        "lucide": "^0.577.0",
        "rolldown": "^1.0.0-beta.39",
        "sharp": "^0.34.0"
    }
}